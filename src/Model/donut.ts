export class Donut {
  active: number;
  completed: number;
  ended: number;

  constructor(active: number, completed: number, ended: number) {
    this.active = active;
    this.completed = completed;
    this.ended = ended;
  }
}
